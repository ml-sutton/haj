describe("These tests interact with our realm database provider", () => {
  it("should run database related tests", () => {
    expect(true).toBe(true);
  });
});
describe("These tests interact with our AsyncStorage database", () => {
  it("should run AsyncStorage related tests", () => {
    expect(true).toBe(true);
  });
});